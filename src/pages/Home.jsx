import React from 'react';
import { Intro } from '../components/Intro';

const Home = (props) => {
    return (
        <div style={{overflow: 'hidden'}}>
            <Intro />
        </div>
    );
}

export default Home;